#VARIANLES DE ENTORNO PARA PRODUCCION CON .ENV
#spring.application.name=${SPRING_APPLICATION_NAME}

#spring.datasource.url=${DB_URL}
#spring.datasource.username=${DB_USERNAME}
#spring.datasource.password=${DB_PASSWORD}
#spring.datasource.driver-class-name=${DB_DRIVER}

#spring.datasource.hikari.connection-timeout=${HIKARI_CONNECTION_TIMEOUT}
#spring.datasource.hikari.validation-timeout=${HIKARI_VALIDATION_TIMEOUT}
#spring.datasource.hikari.maximum-pool-size=${HIKARI_MAX_POOL_SIZE}
#spring.datasource.hikari.minimum-idle=${HIKARI_MIN_IDLE}
#spring.datasource.hikari.idle-timeout=${HIKARI_IDLE_TIMEOUT}
#spring.datasource.hikari.max-lifetime=${HIKARI_MAX_LIFETIME}
#spring.datasource.hikari.auto-commit=${HIKARI_AUTO_COMMIT}

#spring.jpa.hibernate.ddl-auto=${JPA_DDL_AUTO}
#spring.jpa.show-sql=${JPA_SHOW_SQL}
#spring.jpa.properties.hibernate.format_sql=${JPA_FORMAT_SQL}
#spring.jpa.properties.hibernate.dialect=${JPA_DIALECT}

#app.jwt.secret=${JWT_SECRET}
#app.jwt.expiration-ms=${JWT_EXPIRATION}

#stripe.api-key=${STRIPE_SECRET_KEY}
#stripe.publishable-key=${STRIPE_PUBLISHABLE_KEY}

#server.port=${SERVER_PORT}

#logging.level.org.springframework.web=${LOG_LEVEL_SPRING_WEB}
#logging.level.org.hibernate.SQL=${LOG_LEVEL_HIBERNATE_SQL}
#logging.level.com.zaxxer.hikari=${LOG_LEVEL_HIKARI}

#VARIABLES DE ENTORNO SIN .ENV, SOLO PARA DENOSTRACION LO DEJAMOS ASI
spring.application.name=Backend_TallerAutomotriz

spring.datasource.url=jdbc:postgresql://aws-1-us-east-1.pooler.supabase.com:5432/postgres?preparedStatementCacheQueries=0&preparedStatementCacheSqlLimit=0
spring.datasource.username=postgres.ogmnttitkfewhusdoqdn
spring.datasource.password=yaelgaymer22
spring.datasource.driver-class-name=org.postgresql.Driver

spring.datasource.hikari.connection-timeout=60000
spring.datasource.hikari.validation-timeout=30000
spring.datasource.hikari.maximum-pool-size=3
spring.datasource.hikari.minimum-idle=1
spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.max-lifetime=600000
spring.datasource.hikari.auto-commit=true
spring.datasource.hikari.connection-test-query=SELECT 1
spring.datasource.hikari.connection-init-sql=SELECT 1

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true
spring.jpa.properties.hibernate.temp.use_jdbc_metadata_defaults=false
spring.jpa.properties.hibernate.query.plan_cache_enabled=false
spring.jpa.properties.hibernate.query.plan_parameter_metadata_max_size=0

app.jwt.secret=clavesecretamuylargarandom123456789abcdefgh
app.jwt.expiration-ms=86400000

stripe.api-key=${STRIPE_SECRET_KEY:}
stripe.publishable-key=${STRIPE_PUBLISHABLE_KEY:}

server.port=4000

logging.level.org.springframework.web=WARN
logging.level.org.hibernate.SQL=WARN
logging.level.com.zaxxer.hikari=WARN

